name: Runtime Smoke
on:
  push:
    branches: [ main ]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Syntax check iPhone backup core
        run: node --check iphone-backup-v400.js
      - run: npm init -y >/dev/null 2>&1 && npm install playwright@1.48.2 >/dev/null 2>&1 && npx playwright install chromium >/dev/null 2>&1
      - name: Verify live V400 runtime
        run: |
          cat > smoke.js <<'JS'
          const { chromium } = require('playwright');
          const fs=require('fs');
          (async()=>{
            const browser=await chromium.launch({headless:true});
            const page=await browser.newPage();
            const errors=[];
            page.on('pageerror',e=>errors.push(e.message));
            let result={status:'FAIL',version:'unknown',today:false,dev:false,weightHiddenInDev:false,iphoneBackup:false,oldSyncLayersGone:false,noConflictStack:false,deployAttempts:0,errors:[]};
            try{
              let live=false;
              for(let i=1;i<=18;i++){
                result.deployAttempts=i;
                await page.goto('https://ngtzvpv8dz-create.github.io/Master-Of-Disaster/?smoke='+Date.now(),{waitUntil:'networkidle',timeout:60000});
                live=await page.evaluate(()=>!!window.__modIphoneBackupV400);
                if(live) break;
                await page.waitForTimeout(10000);
              }
              if(!live) throw new Error('V400 iPhone backup core not deployed after retry window');
              result.iphoneBackup=true;
              await page.waitForSelector('[data-tab="today"]',{timeout:15000});
              const scripts=await page.evaluate(()=>Array.from(document.scripts).map(s=>s.src||'').join('\n'));
              result.oldSyncLayersGone=!/sync-core-v399|offline-sync-v396|offline-conflict-v398|multidevice-sync-fix/.test(scripts);
              result.noConflictStack=!/conflict-center\.js|conflict-filter-v388|weight-conflict-v392|conflict-actions-v390/.test(scripts);
              await page.click('[data-tab="today"]');
              await page.waitForTimeout(300);
              result.today=await page.locator('[data-tab="today"]').evaluate(e=>e.classList.contains('active'));
              await page.click('[data-tab="dev"]');
              await page.waitForTimeout(500);
              result.dev=await page.locator('[data-tab="dev"]').evaluate(e=>e.classList.contains('active'));
              result.weightHiddenInDev=await page.locator('#weightContainer').evaluate(e=>getComputedStyle(e).display==='none');
              const body=await page.locator('body').innerText();
              const matches=body.match(/V\d{3}/g)||[];
              result.version=matches.includes('V400')?'V400':(matches[0]||'unknown');
              result.errors=errors;
              result.status=(result.today&&result.dev&&result.weightHiddenInDev&&result.iphoneBackup&&result.oldSyncLayersGone&&result.noConflictStack&&result.version==='V400'&&!errors.length)?'PASS':'FAIL';
            }catch(e){result.errors.push(String(e.stack||e));}
            fs.writeFileSync('runtime-status.txt',JSON.stringify(result,null,2));
            console.log(result);
            await browser.close();
            if(result.status!=='PASS') process.exitCode=1;
          })();
          JS
          node smoke.js
      - name: Publish result
        if: always()
        run: |
          git init /tmp/status
          cd /tmp/status
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          cp "$GITHUB_WORKSPACE/runtime-status.txt" . || echo '{"status":"FAIL","errors":["runtime-status missing"]}' > runtime-status.txt
          git add runtime-status.txt
          git commit -m "Runtime smoke result"
          git branch -M runtime-status
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin runtime-status
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
