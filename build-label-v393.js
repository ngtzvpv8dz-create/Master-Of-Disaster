/* V401 · obsolete build label shim intentionally disabled.
   Build metadata is now controlled only by build-label-v400.js.
   Keeping this file as a harmless no-op avoids stale cached references breaking page load.
*/
(function(){})();
